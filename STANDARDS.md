# 学术排版标准考察

本模板的每一处版式决定都尽量对应到一条可查证的标准或成文惯例。下面按"规范来源 → 具体要求 → 本模板的落实方式"三段列出，方便你核对与改写。

> 说明：本文档依据编写者掌握的规范条文整理，标准编号与年份已尽量写全，**建议按编号自行核对原文**（尤其是投稿到具体期刊时，期刊自身的 Style Guide 优先级高于通用国标）。

---

## 一、中文学术出版的强制性/推荐性国家标准

| 标准编号 | 名称 | 管到什么 |
|---|---|---|
| GB/T 7713.1—2006 | 学位论文编写规则 | 前置部分 / 主体部分 / 结尾部分的**结构顺序** |
| GB/T 7713.2—2022 | 学术论文编写规则 | 期刊论文的题名、摘要、关键词、正文层次 |
| GB/T 7714—2015 | 信息与文献 参考文献著录规则 | **参考文献格式**（顺序编码制 / 著者—出版年制） |
| GB/T 3100—1993 | 国际单位制及其应用 | 单位符号的书写 |
| GB/T 3101—1993 | 有关量、单位和符号的一般原则 | **量符号的正斜体** |
| GB/T 3102（系列）—1993 | 量和单位 | 各学科量符号的规定写法 |
| GB/T 15834—2011 | 标点符号用法 | 中文标点 |
| GB/T 15835—2011 | 出版物上数字用法 | 何时用阿拉伯数字、何时用汉字数字 |
| CY/T 154—2017 | 中文出版物夹用英文的编辑规范 | 中英混排的空格、标点、字体 |

### 1.1 结构顺序（GB/T 7713 系列）

学术论文的规范顺序是：

```
题名 → 作者 → 作者单位 → 摘要 → 关键词 → 中图分类号/文献标志码
→ 正文（引言 → 材料与方法 → 结果 → 讨论 → 结论）
→ 致谢 → 参考文献 → 附录
```

要点：

- **致谢在参考文献之前**，附录在参考文献之后。这一点最常被写错。
- 摘要应是"报道性摘要"，能独立于全文存在，不出现"本文"、"作者"这类自指表述，不引用文献编号。
- 关键词 3–8 个，中英文一一对应，用分号分隔。
- 中文题名一般不超过 20 字，不用非公知缩写。

**模板落实**：`article/main.tex` 里的 `\input` 顺序已经按此排好，致谢/附录各自独立成文件，不要随意调换顺序。

### 1.2 参考文献（GB/T 7714—2015）

两种著录体系，二选一，全文统一：

- **顺序编码制**：正文中标 `[1]`、`[2-4]`，文后按引用先后排序。理工科绝大多数期刊用这个。
- **著者—出版年制**：正文中标 `(张三, 2020)`，文后按著者字顺排。人文社科常用。

必须标注**文献类型标识码**，这是 GB/T 7714 区别于 IEEE/APA 的最显著特征：

| 代码 | 类型 | 代码 | 类型 |
|---|---|---|---|
| `[J]` | 期刊文章 | `[D]` | 学位论文 |
| `[M]` | 专著 | `[R]` | 报告 |
| `[C]` | 论文集 | `[S]` | 标准 |
| `[A]` | 析出文献 | `[P]` | 专利 |
| `[N]` | 报纸 | `[EB/OL]` | 网络电子资源 |

载体标识：`[J/OL]` 表示网络期刊，`[DB/OL]` 数据库，`[CP]` 计算机程序。

其他易错点：

- 著者不超过 3 位全列，超过 3 位列前 3 位后加"等"（英文加 "et al."）。
- 英文著者姓在前、名缩写在后且**不加点**：`ZHANG S M`（GB/T 7714 风格）。
- 电子资源必须给出**引用日期**和 URL：`[2026-07-26]. https://...`。

**模板落实**：默认 `bibstyle=gb7714`，走 `biblatex` + `biber` + `biblatex-gb7714-2015` 宏包，上述规则由样式文件自动处理。投英文期刊时把类选项改成 `bibstyle=ieee` 即可整体切换，不需要动 `.bib`。

### 1.3 量和单位的正斜体（GB/T 3101、GB 3100）

这是评审最容易挑刺、也最容易系统性写错的地方：

| 对象 | 字体 | 例 |
|---|---|---|
| 量符号（变量） | **斜体** | *m*、*T*、*E* |
| 单位符号 | **正体** | kg、K、J |
| 数学常数 | **正体** | e、π、i |
| 微分算子、求和号 | **正体** | d*x*、∑ |
| 函数名 | **正体** | sin、log、exp、max |
| 矢量、张量 | **粗斜体** | ***F***、***σ*** |
| 矩阵 | **粗正体或粗斜体**（全文统一） | **A** |
| 化学元素符号 | **正体** | Fe、H₂O |
| 下标：代表量的 | 斜体 | *C*<sub>*p*</sub>（*p* 是量） |
| 下标：说明性文字的 | 正体 | *E*<sub>max</sub>、*T*<sub>in</sub> |

数值与单位之间**留一个窄空格**：`5 kg`、`25 °C`；例外是平面角的度分秒和百分号：`30°`、`50%` 不留空格。

**模板落实**：

- `mathstyle=iso` 类选项开启 `unicode-math` 的 ISO 规则（连大写拉丁字母和大写希腊字母作变量时也斜体）。
- 单位一律用 `siunitx` 的 `\qty{5}{\kg}` / `\unit{\m\per\s}` 写，间距和正体由宏包保证，不要手写 `5 kg`。
- 下标说明文字用 `\subtext{max}`（模板宏，自动转正体），不要写 `E_{max}`。

### 1.4 数字用法（GB/T 15835）

- 计量数字、编号一律阿拉伯数字：`3 个样本`、`第 5 章`。
- 概数、成语、固定词组用汉字：`三四天`、`三分之一`、`第一线`。
- 5 位以上数字分节：`1 234 567`（用窄空格，不用逗号）。
- 数值范围用浪纹线：`5~10 kg`，且单位只写一次。

### 1.5 中英混排（CY/T 154—2017）

- 中英文之间**不手动加空格**——由 `xeCJK` 的 `\xeCJKsetup{CJKglue}` 自动插入可伸缩间距，手打空格会破坏对齐。
- 夹用的英文句子用英文标点，中文句子内嵌的英文单词后仍用中文标点。
- 中文里的括号用全角 `（）`，纯英文内容的括号用半角 `()`。

---

## 二、国际通用规范

### 2.1 ISO 80000-2:2019《数学符号》

与 GB/T 3101 基本等价（国标就是等同采用），额外明确：

- 集合符号 ℝ、ℤ、ℕ 用**黑板粗体正体**。
- 转置写作上标 T 正体：*A*ᵀ。
- 张量用无衬线粗斜体。
- 区间、绝对值、范数的定界符大小随内容自适应（LaTeX 用 `\left...\right` 或 `mathtools` 的 `\DeclarePairedDelimiter`）。

**模板落实**：`common/acadmath.sty` 用 `\DeclarePairedDelimiter` 定义了 `\abs`、`\norm`、`\inner`、`\set`，自带 `*` 号变体自动缩放。

### 2.2 图表规范（IEEE Editorial Style Manual、ACM Master Template 共识）

- **表题在表上方，图题在图下方**。这是跨所有主流规范的一致要求。
- 表格用**三线表**（顶线、栏目线、底线），不画竖线，不画多余横线。中文期刊通常还要求表题中英文对照。
- 图必须是矢量格式（PDF/EPS），位图不低于 600 dpi。
- 坐标轴必须标注"量名 / 单位"，规范写法是 `t / s`（斜杠形式）而非 `t (s)`——GB/T 3101 推荐斜杠。
- 子图编号用 `(a) (b) (c)`，标注在子图下方。

**模板落实**：`booktabs` 提供 `\toprule/\midrule/\bottomrule`；`caption` 宏包已配置 `position=top`（表）/ `position=bottom`（图）；实际标题位置仍由正文中 `\caption` 的顺序决定；`subcaption` 负责子图。

### 2.3 交叉引用与超链接

- 引用应带类型词：`如图 3 所示`、`见式 (5)`、`Table II shows`。
- 用 `cleveref` 自动生成类型词，避免"图 3" / "图3" / "Fig. 3" 混用。
- `hyperref` 必须**在 `cleveref` 之前**加载，否则引用失效。
- PDF 元数据（标题、作者、关键词）应写入，便于检索。

### 2.4 盲审 / 双盲送审

多数会议（NeurIPS、ICML、ACL）与国内学位论文盲审要求**匿名化**：隐去作者、单位、致谢、基金号，自引改第三人称。

**模板落实**：类选项 `anonymous` 一键开启，自动隐去 `setup.tex` 中的作者/单位/基金/致谢，正文不用改。

---

## 三、学术报告（Beamer）的成文惯例

这一块没有国家标准，以下是主流会议 Speaker Guideline 与学界共识的交集：

- **16:9**：现在会场投影与线上会议的默认比例，4:3 已基本淘汰。模板默认 `aspectratio=169`。
- **字号下限**：正文不小于等效 20 pt。Beamer 在 16:9 下 `11pt` 基准约等于此，因此不要为了塞内容而调小。
- **一帧一论点**：标题应是**结论句**（"方法 A 将误差降低 37%"）而非名词短语（"实验结果"）。
- **克制动画**：只用 `\pause` / `overlay` 做逐步揭示，不用转场特效。
- **导航符号去掉**：Beamer 默认右下角的导航按钮在投影时毫无用处，一律 `\setbeamertemplate{navigation symbols}{}`。
- **页码用 n/N**：便于听众提问时定位。
- **备用页不计入总数**：用 `appendixnumberbeamer`，否则 "12/45" 会误导听众以为还有很久。
- **图优于文字**，文字优于表格；确需上表则只保留关键行。
- **参考文献**：正文页角标出 `[1]` 简引，末尾单独一页列全。

**模板落实**：以上全部在 `beamer/beamerthemeAcad.sty` 及其四个子主题文件中实现，`frames/` 下每个文件是一个逻辑段落。

---

## 四、本模板刻意做出的取舍

| 取舍 | 理由 |
|---|---|
| 引擎用 **XeLaTeX** | 中文与 OpenType 字体支持最成熟；ustcthesis 亦以此为默认 |
| 数学用 **unicode-math** 而非 newtxmath | 只有它能通过 `math-style=ISO` 直接落实 GB/T 3101 的正斜体规则 |
| 文献用 **biblatex + biber** 而非 bibtex | 中英文混排文献的排序与 et al. 处理明显更稳；样式切换只改一个选项 |
| 样式全部收在 `.cls` / `.sty` | 正文文件里不出现任何格式命令，改版式不动正文，符合内容与表现分离 |
| 不预设期刊模板 | 各期刊 Style 差异过大；本模板对齐**通用国标**，投稿时再套刊方模板 |

---

## 五、投稿前自查清单

- [ ] 致谢在参考文献前，附录在参考文献后
- [ ] 摘要不含文献编号、不含"本文"自指
- [ ] 关键词中英文数量一致、一一对应
- [ ] 所有变量斜体、所有单位正体，函数名 `\sin` 而非 `sin`
- [ ] 所有单位经 `siunitx` 输出，数值与单位间距一致
- [ ] 表题在上、图题在下，表为三线表
- [ ] 坐标轴标注为"量 / 单位"形式
- [ ] 图为矢量格式
- [ ] 参考文献均有文献类型标识码，电子资源有引用日期
- [ ] 交叉引用全部走 `\cref`，编译无 `??`
- [ ] 全文无 Overfull hbox 警告（用 `make check` 检查）
- [ ] 盲审版已开 `anonymous` 并确认 PDF 元数据也不含作者名
